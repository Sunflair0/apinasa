// import React from "react";

 function VentureInfo2(){ 

<<<<<<< HEAD
    return (
        <div>
        
        <button className="ventBtn">Click to place order</button>
            </div>
    );
}
=======
LeftbarMenu.map((cube, vent_id)=> {
return (
<li key={vent_id} item={tours}>
<Link to={vent_id.description}>
{"img.vent_id"}
<span>{cube.title}</span>
</Link>
</li>
);
})



<div>
let bigCube = vent_id.map(tour.length) => ({img:("link"), value: ventures.vent_id, text: ventures.description, price: ventures.cost} 

<button className="ventBtn">Click to place order</button>)
            </div>





 const VentureInfo2 =({item})=> {
    return (
        <div>
            {item[index].map(item => {
           <div className="venture">   
           <img alt="menu with planets as an icon"src={item.icon}/> 
            <p>{item.title}</p>
            <h4>{item.content}</h4></div>
            })}
>>>>>>> css-fix-second-menu

 export default VentureInfo2;
